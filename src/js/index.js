const result = "This is paper contains the writing done with the pencil. hard-coded";
const paperEl = document.getElementById('paper');

paperEl.innerHTML = result;

console.log("write with the pencil");